# Tract {tract_id}

Below are every recorded instrument affecting tract `{tract_id}`, in chronological order. Produce the per-tract section of the draft title report in the exact format and style specified in your system prompt.

**Effective Date:** {effective_date}

## Events for tract {tract_id}

{events}

## L&E carve-outs bound to this tract

{le_rows}

## Job context

{job_context}

# Task

Produce the report section for tract `{tract_id}` per the output format defined in your system prompt. Begin with `=== TRACT {tract_id} ===` and end without commentary.

The CHAIN OF TITLE section comes first and must list every event above, oldest first, with a one-clause effect-on-title summary for each.
